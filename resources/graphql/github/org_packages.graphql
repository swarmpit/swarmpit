query OrgPackages($login: String!) {
    organization(login:$login) {
        packages(first:100, packageType:DOCKER) {
            nodes {
                id
                name
                repository {
                    owner {
                        login
                    }
                }
            }
        }
    }
}