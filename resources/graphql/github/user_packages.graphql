query UserPackages($login: String!) {
    user(login: $login) {
        packages(first: 100, packageType: DOCKER) {
            nodes {
                name
                repository {
                    nameWithOwner
                }
            }
        }
    }
}